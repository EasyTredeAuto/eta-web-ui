import styled from "styled-components"

export const BoxNotification = styled.div`
  width: 100%;

  & > p:first-child {
    font-size: 18px;
    font-weight: bold;
    color: #0033FF;
  }

  & > p:last-child {
    color: #888;
  }
`
